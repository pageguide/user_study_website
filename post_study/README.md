Place the exported post-study questionnaire CSV here as:

```text
post_study/post_study.csv
```

The Visualization tab loads it automatically. You can still upload another CSV in the dashboard to
replace it for that session.

The dashboard also checks for a root-level `post_study.csv`, so either location works.
